# `wechat-points-stock` 优化与改进路线图

> 基线：`main` / `aabf612a47eb13f28f7f83f8ab4129dbf791628f`，审查日期 2026-09-18。本文档先记录基线，再持续记录实施进度；未列为“已落地”的验收标准仍然有效。仓库整体上下文见根目录 `CODEX_REPO_MEMORY.md`。

## 1. 文档目标与使用方法

本路线图用于指导后续迭代，优先解决会导致凭据泄露、越权访问、数据错误或恢复失败的问题，再处理性能、可维护性和体验。建议一项或一组强相关事项对应一个小 PR，避免安全、迁移、性能和 UI 重构混在一次发布中。

### 1.1 当前工作区实施进度（2026-09-18）

本轮已经开始按 P0 → P1 顺序落地；标记为“已完成”的项目已经完成当前仓库范围内的验收，其他部署环境仍按各自发布流程执行轮换或验证：

| 项目 | 当前状态 | 已落地内容 | 尚未闭环 |
|---|---|---|---|
| OPT-001 | 已完成（当前仓库） | `INGEST_TOKEN`、启动时拒绝默认/过短凭据、`API_TOKEN` 一版兼容、`.env.example`、本机凭据已轮换、脚本/上报文档已统一命名、CI secret scan 已启用并阻断失败；`main` 可达历史已移除 `.env`、数据库运行时文件和 `venv/` | 若有其他已部署环境，需按部署平台替换其旧凭据；GitHub 之外的镜像/缓存按平台回收策略处理 |
| OPT-002 | 部分完成 | `web`/`stock` router 统一 `require_ui_access`、常量时间比较、前端 401 清会话并回到访问页 | 会话化、失败限流、审计 |
| OPT-003 | 已完成 | 单程序库存详情的最新积分/现金查询补齐 `program_id` join 条件 | 稳定 `(report_time, id)` tie-breaker 可随历史查询优化一起做 |
| OPT-004 | 第一阶段 | 导入大小限制、SQLite 完整性/核心表校验、维护态、WAL checkpoint、同卷 `os.replace`、时间戳回滚副本 | 跨进程维护锁、完整恢复/回滚集成测试、调度优雅暂停 |
| OPT-005 | 第一阶段 | 临时 SQLite、鉴权、路径穿越、库存分页和跨程序余额回归测试；已补 `requirements-dev.txt`、GitHub Actions CI 与失败即阻断的 secret scan | 前端测试/lint |
| OPT-007 | 第一阶段 | 图片上传增加 MIME/文件头校验与 10 MiB 限制；数据库导入增加 256 MiB 上限和 SQLite 校验 | 更细的请求体/批量限制、统一错误审计 |
| OPT-009 | 第一阶段 | `report_time + id` 稳定最新记录排序、同时间戳最大 `id` 选择、最新余额查询组合索引、跨程序隔离回归测试 | 窗口化/快照表查询、日志批量裁剪、可重复压测基准 |
| OPT-010 | 第一阶段 | 库存中心 SQL 分页/筛选、汇总元数据、第一页跨组件缓存、隐藏/下架首屏懒加载、下架抽屉分页、菜单 hover/focus 预加载库存 chunk、组合索引 | revision/ETag、冷/热 p95 基准、虚拟网格 |
| OPT-011 | 第二阶段 | 大部分 `web.py` 同步数据库/网络 I/O 路由改为普通 `def`；FastAPI lifespan 管理调度启动/关闭；Bark/QingLong 使用可停止 Event、join 和锁释放 | 外部 HTTP 连接复用与有限重试、慢请求并发基准 |
| OPT-012 | 第一阶段 | `/health/live`、`/health/ready`、Compose ready healthcheck；API/健康请求增加安全 request ID、响应头和耗时/状态 key-value 日志 | 高风险审计事件、指标导出、统一 JSON 日志格式 |
| OPT-013 | 第一阶段 | Dockerfile 改为 Node 构建阶段 + Python 运行阶段，镜像自包含 `frontend/dist`；运行用户改为非 root，代码目录只读；Compose 保留端口、环境变量、健康检查和数据/日志/上传挂载，移除宿主机 `frontend/dist` 依赖 | Docker 实际 build/config、非 Windows bind mount 权限和 systemd 一致性仍需目标环境验证；基础镜像 digest 固定暂未处理 |

后续继续从当前工作区向下推进；如果某项需要数据库迁移或部署改动，会先保留兼容路径并单独验证旧库。

> 本轮安全闭环记录（2026-09-18）：新增 `.gitleaks.toml`，CI 使用 `gitleaks/gitleaks-action@v3` 配合完整 checkout（`fetch-depth: 0`）扫描提交/工作区，secret scan 失败会使 CI job 失败；配置只对 `.env.example` 和 `tests/test_regressions.py` 中列明的占位符做规则级例外。已从 `main` 可达历史移除 `.env`、`data/database.db`、`data/database.db.bak`、根目录数据库、`venv/` 等运行时内容，并将本机旧 `API_TOKEN` 轮换为新的 `INGEST_TOKEN`（值未进入 Git 或日志）。

> 本轮运行可靠性验收记录（2026-09-18）：最新余额查询在相同 `report_time` 下按最大 `id` 稳定取值，并补充程序/账号组合索引与内存 SQLite 回归测试；后台调度器已接入 lifespan，关闭时通过 `Event` 唤醒并在有限时间内 join；API/健康请求返回 `X-Request-ID` 并记录 route、status、duration_ms。OPT-009 的历史窗口化查询、OPT-011 的外部 HTTP 连接复用/重试、OPT-012 的审计和指标仍未闭环。

优先级定义：

- **P0**：上线或暴露到不可信网络前必须处理；可能导致凭据、数据完整性或核心结果错误。
- **P1**：近期迭代处理；已经影响可靠性、扩展性、故障定位或开发效率。
- **P2**：中期结构优化；当前可运行，但继续堆功能会显著增加维护成本。
- **P3**：体验或长期演进项；根据实际规模和业务需求决定是否实施。

工作量定义：

- **S**：半天以内
- **M**：约 1–2 个工程日
- **L**：约 3–5 个工程日
- **XL**：一周以上，建议拆分

## 2. 执行摘要

| ID | 优先级 | 主题 | 主要收益 | 工作量 |
|---|---|---|---|---|
| OPT-001 | P0 | 明确上报 Token 去留并移除 Git 中的 `.env` | 保住写接口边界并消除已知凭据泄露 | S–M |
| OPT-002 | P0 | 统一 UI API 的服务端鉴权边界 | 防止绕过前端访问页直接读写数据 | M–L |
| OPT-003 | P0 | 修复单程序库存最高余额跨程序串数 | 保证可兑换判断正确 | S |
| OPT-004 | P0 | 重构 SQLite 数据库恢复流程 | 防止恢复时与 WAL/请求并发造成损坏 | M–L |
| OPT-005 | P0 | 建立最小后端回归测试与 CI | 为后续所有优化提供安全网 | M–L |
| OPT-006 | P1 | 引入正式数据库迁移、外键与级联策略 | 结束请求期 DDL，防止孤儿数据 | L |
| OPT-007 | P1 | 强化输入、上传和批量请求限制 | 降低误数据、DoS 和恶意文件风险 | M |
| OPT-008 | P1 | 统一 UTC/Asia-Shanghai 时间语义 | 修复“今日未报/今日变化”边界问题 | M |
| OPT-009 | P1 | 优化积分历史与最新余额查询 | 避免数据增长后内存和响应时间失控 | L–XL |
| OPT-010 | P1 | 库存中心真实分页与首屏性能 | 消除每次进入库存页的全量传输和白屏等待 | L |
| OPT-011 | P1 | 修正同步路由的阻塞模型与任务生命周期 | 避免单 worker event loop 被同步 I/O 卡住 | M–L |
| OPT-012 | P1 | 健康检查、结构化日志、审计与指标 | 提升部署和故障定位能力 | M–L |
| OPT-013 | P1 | 可复现、非 root、自包含部署 | 降低部署漂移和容器权限风险 | M–L |
| OPT-014 | P2 | 拆分大型 Vue 页面与共享业务逻辑 | 降低 UI 修改回归风险 | XL（分步） |
| OPT-015 | P2 | 统一前端请求、缓存、响应式断点和错误处理 | 改善一致性和移动端行为 | M–L |
| OPT-016 | P2 | 规范金额、标签、身份映射等数据模型 | 提升长期数据一致性 | L–XL |
| OPT-017 | P2 | 补齐前端测试、lint、格式化和依赖治理 | 提升交付质量 | L |
| OPT-018 | P2 | 收敛文档漂移与废弃代码 | 减少后续误判 | S–M |

## 3. P0：立即处理

### OPT-001：明确上报 Token 去留并移除 Git 中的 `.env`

**现状证据**

- 基线检查曾发现 `.gitignore` 已忽略 `.env`，但 `git ls-files .env` 仍能看到该文件，说明它已被提交过；本轮已验证当前 `main` 的 `git ls-files .env` 为空，且可达历史不再包含该路径。
- 文件内存在非默认 `API_TOKEN`；本文档不复述其值。
- `app/config.py:7` 在环境变量缺失时回退到可预测的 `default_token`。
- `API_TOKEN` 经 `app/dependencies.py:verify_token()` 校验 `Authorization: Bearer <token>`，当前只保护 3 个外部写接口：
  - `POST /api/v1/qinglong/report`：写入账号、程序及积分/现金历史。
  - `POST /api/v1/stock-report`：写入商品和库存历史；由于请求代表全量快照，本次未出现的在架商品会被自动标记为下架。
  - `POST /api/v1/upload/image`：向服务器上传目录写文件，并可更新商品本地图片路径。
- 前端 Axios 只发送 `X-Access-Key`，不会使用 `API_TOKEN`；它也不是青龙 OpenAPI 临时 token，更不是程序的 `auth_type=token`。因此它的准确角色是“采集脚本到本服务的机器间上报凭据”。
- `scripts/api_template.py` 和库存上报文档要求外部脚本携带该 Bearer 凭据。
- `docker-compose.yml` 当前使用 `5711:5711` 发布端口；没有额外网络限制时，可访问宿主机该端口的客户端就能尝试调用上述接口。

**风险**

- 仓库读权限等价于外部上报接口写权限。
- 基线时即使执行 `git rm --cached .env`，历史提交中的 token 仍可能可见；本轮已重写当前 `main` 可达历史并轮换本机凭据，旧值不再作为当前应用凭据使用。
- 部署漏配环境变量时会静默使用公共默认值。
- 若只删除路由上的 `verify_token` 而没有建立网络边界，上述 3 个接口会变成匿名写接口：攻击者可伪造积分、篡改库存/上下架状态，或持续上传文件占用磁盘。
- `X-Access-Key` 目前是可关闭、由部分 `web.py` 路由手工检查的 UI 访问锁，不能直接视作这 3 个接口的等价替代品。

**建议方案**

先根据部署边界明确二选一，不能只因为前端没有使用它就直接删除：

1. **保留机器鉴权（当前推荐）**
   - 当 `5711` 可被局域网、公网或任何不完全可信客户端访问时，保留这层鉴权。
   - 将服务端变量重命名为语义更清楚的 `INGEST_TOKEN`；如需平滑升级，可让 `API_TOKEN` 只作为一个版本的弃用兼容项。
   - 删除 `default_token` 回退值；凭据缺失、仍为默认值或强度不足时拒绝启动。
   - 使用 `secrets.compare_digest()` 比较凭据，继续只保护上述 3 个接口，不与 UI 会话或青龙 OpenAPI token 混用。
   - 若图片上传已无调用方，优先下线该接口；否则增加类型、大小和内容校验，必要时为上传能力设置独立权限。

2. **移除应用层上报 Token（仅限已有可信网络边界）**
   - 先把容器端口绑定到 `127.0.0.1:5711:5711`，或确保 VPN、反向代理鉴权、IP allowlist、mTLS 等边界已生效，并阻止绕过代理直连宿主机端口。
   - 验证不可信网络无法直接访问 3 个写接口后，再移除 `verify_token`、`API_TOKEN` 配置及脚本中的 Bearer 头。
   - 同步修改 `app/config.py`、`app/dependencies.py`、两个路由文件、`scripts/api_template.py`、README 和上报文档，避免形成“代码已匿名、文档仍称有鉴权”的漂移。
   - 如果希望统一为 `X-Access-Key`，必须先把它改造成不可随意关闭、服务端统一强制执行且适合外部脚本使用的正式鉴权机制，不能直接复用当前手工校验方式。

无论选择哪条路线，都要完成以下动作：

3. 新建 `.env.example`，只保留键名和安全占位符。
4. 从 Git 索引移除 `.env`，但保留本机文件；检查远端历史和仓库可见范围后决定是否清理历史。
5. 若保留 Token，立即生成并部署新值，使仓库中已出现的旧值失效；若移除 Token，则确认所有环境均已部署不依赖该值的新版本。
6. 在 CI 加 secret scanning，阻止 `.env`、Token、QingLong Secret、Bark Key 再次提交。

**验收标准**

- `git ls-files .env` 无输出，`.env.example` 不包含真实值。
- 设计文档明确记录最终采用“应用层机器鉴权”还是“可信网络边界”，并列出部署前提。
- 在任一方案下，3 个写接口都不能被不可信网络匿名调用。
- 若保留 Token：新 Token 生效，旧 Token 请求返回 401；缺少 `INGEST_TOKEN` 时服务拒绝启动，不再接受 `default_token`。
- 若移除 Token：宿主机端口无法绕过代理/ACL 直连，并有自动化或部署检查证明未授权来源无法调用写接口。
- README、脚本模板和库存上报文档准确区分上报凭据、`X-Access-Key` 和青龙 OpenAPI token。
- CI 能拦截测试用的伪泄露 token。

### OPT-002：统一 UI API 的服务端鉴权边界

**现状证据**

- `X-Access-Key` 由 `web.py:verify_access_or_raise()` 手工调用。
- 设置、备份、Bark、QingLong 管理接口会检查访问密钥；账号、积分、程序和库存的大多数读写接口没有检查。
- 前端路由守卫只阻止页面导航，无法阻止调用者直接访问 `/api/v1/accounts`、`/programs`、删除接口等。
- 密钥以明文存于 SQLite，并长期放在浏览器 `localStorage`。
- `/access/verify` 没有速率限制或失败审计。

**风险**

- 开启“访问保护”后仍可绕过 SPA 直接读取和修改数据。
- XSS、共享浏览器或本地调试工具可读取长期保存的密钥。
- 暴力尝试密钥没有成本控制。

**建议方案**

分两步实施：

1. **短期最小闭环**
   - 把访问检查抽到独立依赖，例如 `require_ui_access()`。
   - 在 UI 业务 router 上统一挂依赖，明确豁免 `/access/status`、`/access/verify`、Bearer 上报和静态资源。
   - 所有修改/删除接口必须受保护；读取接口是否允许匿名由配置明确决定，不能依赖“前端看不到”。
   - Axios 响应拦截器统一处理 401：清理会话并跳转访问页。
   - 给验证接口增加 IP/设备维度的失败计数、指数退避或反向代理限流。

2. **中期会话化**
   - 验证成功后签发短期、可失效的服务端会话；优先 HttpOnly + SameSite Cookie，避免把原始密钥长期放在 localStorage。
   - 数据库只保存密钥哈希，不保存可逆明文；修改密钥时使旧会话失效。
   - 对数据库恢复、删除程序、批量修改 QingLong 定时等高风险操作记录审计日志。

**验收标准**

- 保护开启时，不带凭据直接访问任一受保护读写 API 均返回 401/403。
- Bearer 上报不受 UI 会话机制影响。
- 保护关闭时保持现有易用性。
- 密钥不会通过 GET API、日志或错误信息返回。
- 自动化测试覆盖“关闭保护、开启保护、正确凭据、错误凭据、过期会话”五类场景。

### OPT-003：修复单程序库存最高余额跨程序串数

**现状证据**

`app/routers/web.py:1810-1823` 和 `1924-1930`：子查询按目标 `program_id` 找每个账号的最新时间，但外层 `PointsHistory` 只按 `wechat_id + report_time` 连接，没有再次限定 `PointsHistory.program_id == program_id`。

若同一账号在同一批次时间同时上报多个程序，外层聚合可能把其他程序的积分或现金带进来，进而影响库存弹窗的“可兑换”判断。批量 helper `get_program_max_user_points_map()` 已正确连接 `program_id`，两处语义不一致。

**建议方案**

- 在积分和现金外层查询的 join 条件中都加入 `PointsHistory.program_id == program_id`，或直接复用已正确的批量 helper。
- 对同一 `report_time` 的重复记录增加稳定 tie-breaker（例如最大 `id`），避免“最新记录”不确定。

**验收标准**

- 构造同一账号、同一时间、两个程序、不同余额的测试数据，接口只能返回目标程序余额。
- `GET /programs/{id}/stock` 与 `/programs` 卡片上的最高积分/现金一致。
- 积分和现金两个维度均有回归测试。

### OPT-004：重构 SQLite 数据库恢复流程

**现状证据**

- `web.py:1277-1361` 会关闭当前 session、`engine.dispose()`，随后使用 `shutil.copy2()` 覆盖主数据库文件。
- 注释写“atomically replace”，实际并非原子替换。
- 应用启用了 WAL，但恢复过程没有统一阻止后台 Bark/QingLong 线程或其他请求继续访问，也没有明确处理 `-wal` / `-shm` 文件。
- 上传文件大小没有上限；回滚副本只有一个固定 `.pre_restore`。

**风险**

- 恢复期间仍有连接写入，可能产生不一致、旧 WAL 重放或恢复后连接状态异常。
- 复制中途进程退出会留下半写文件。
- 无限上传可耗尽磁盘。

**建议方案**

1. 引入全局“维护模式/恢复锁”：开始恢复后拒绝新业务请求，暂停调度线程并等待在途事务结束。
2. 限制上传大小，校验扩展名、SQLite header、`integrity_check`、核心表和 `schema_version`。
3. 对现库执行 WAL checkpoint，关闭连接池，备份主库及相关 sidecar。
4. 把验证后的数据库复制到目标目录的临时文件，再用同卷 `os.replace()` 原子替换。
5. 明确清理或隔离旧 `-wal` / `-shm`，恢复后重新建连并跑只读 smoke check。
6. 回滚副本按时间戳命名并支持保留策略；UI 明确提示恢复期间不可用和是否需要重启。
7. 更稳妥的部署可将“导入”改为离线管理命令，由运维在停止服务后执行。

**验收标准**

- 恢复中业务请求返回 503/维护提示，不会继续写库。
- 中断替换前，原数据库仍完整可用；成功后新库通过 `integrity_check`。
- WAL 模式下并发写入测试不产生损坏或旧数据回放。
- 超过大小限制的文件在写满磁盘前被拒绝。
- 有自动化测试覆盖有效备份、非 SQLite、缺表、损坏库和恢复回滚。

### OPT-005：建立最小后端回归测试与 CI

**现状证据**

基线时仓库没有 pytest、前端测试、lint、formatter 或 CI 配置；当前工作区已补 pytest 回归测试、`compileall`、前后端 CI 和 secret scan。前端测试/lint/formatter 仍未配置，后续查询、迁移和鉴权优化仍需继续扩充覆盖范围。

**首批测试范围**

1. Bearer token：缺失、错误、正确。
2. 积分/现金 schema：只积分、只现金、双报、0、数字字符串、空值、非法值。
3. 手机号账号解析：已绑定、未绑定、昵称不被空值覆盖。
4. 库存全量快照：新增、变化、下架、重新上架、空快照不全量下架、手动隐藏保持不变。
5. OPT-003 的跨程序同时间戳场景。
6. access-key 开关和所有受保护路由。
7. QingLong 名称匹配与 cron 解析纯函数。
8. Asia/Shanghai 跨日边界与“今日未报”。
9. 备份导出/恢复校验（恢复写盘可放集成测试）。

**建议基础设施**

- `pytest` + FastAPI `TestClient`，每个测试使用临时 SQLite 数据库。
- 测试配置通过依赖注入/应用工厂传入，不读真实 `.env` 和 `data/database.db`。
- CI 至少执行：后端单测、`compileall`、前端 `npm ci && npm run build`。
- 给关键上报/库存逻辑设置覆盖率门槛，先从 70% 起步，再逐步提高；不要追求全仓一次达标。

**验收标准**

- 测试不会访问真实 QingLong、Bark、生产数据库或公网。
- PR 必须通过测试和前端构建才能合并。
- P0 修复都带失败前/成功后的回归用例。

## 4. P1：数据可靠性与后端结构

### OPT-006：正式迁移、外键和删除策略

**现状证据**

- `ensure_*_columns()` 在大量请求入口检查表结构，运行时执行 DDL。
- `database.py` 设置 WAL/busy timeout/synchronous，但没有 `PRAGMA foreign_keys=ON`。
- `StockHistory` 没有外键；删除程序时只删除 `PointsHistory` 和 `Product`，不删除 `StockHistory`，会留下孤儿历史。
- `SystemSettings` 逻辑上是单例，但数据库没有强制单行。

**建议方案**

1. 引入 Alembic（或至少自有 `schema_version` + 顺序迁移器），迁移在启动前/部署阶段执行。
2. 从请求热路径移除 schema introspection 和 DDL；保留一段兼容期后删除 lazy migration。
3. SQLite 连接开启 `PRAGMA foreign_keys=ON`。
4. 明确级联规则：
   - 删除账号 → 删除其 `PointsHistory`。
   - 删除程序 → 删除 `PointsHistory`、`Product`、对应 `StockHistory`。
   - 商品历史是审计数据还是随程序删除，需在迁移前确认；若保留则增加明确的软删除/归档语义。
5. 为 `SystemSettings` 固定主键或加唯一单例约束。
6. 用旧版本数据库 fixture 验证从每个受支持版本升级到最新。

**验收标准**

- 正常 API 请求不再执行 `ALTER TABLE` 或反射整张表结构。
- 删除实体后无未定义的孤儿行。
- 新库和至少一个旧库样本均能一键迁移；迁移失败不会启动应用。
- 每个迁移有升级验证和备份/回滚说明。

### OPT-007：强化输入、上传和批量限制

**现状证据**

- `schemas.py` 的字符串没有最小/最大长度和去空白约束。
- `stock`、`points`、`cash` 可传负数；`float("nan")`、无穷值也没有明确拒绝。
- 上报数组、批量账号、每程序商品数没有请求级上限。
- 图片上传直接沿用用户文件扩展名，没有 MIME、内容、尺寸、大小限制，且同步写盘发生在 async route 中。
- 数据库导入没有大小限制。

**建议方案**

- 使用 Pydantic `Field`/`StringConstraints`：ID/名称去空白、长度上限、非空；库存/价格设合理范围，所有数值必须 finite。
- 对请求 body、账号数、每账号程序数、每次商品数设置可配置上限；超过上限返回 413/422。
- 库存允许负数与否必须定义清楚；一般应拒绝负库存、负积分和负现金。
- 图片只允许明确 MIME（如 JPEG/PNG/WebP），读取 magic bytes，不信任扩展名；服务端生成固定扩展名，限制文件大小和像素尺寸。
- 上传失败时删除临时文件；替换图片时按策略回收旧文件。
- 为反向代理和应用同时配置 body 大小限制。

**验收标准**

- 空 ID、超长名称、负值、NaN/Infinity、超大数组有稳定错误响应。
- 伪装扩展名和超限文件不会写入 `static/uploads`。
- 合法旧脚本仍兼容。

### OPT-008：统一 UTC 与 Asia/Shanghai 时间语义

**现状证据**

项目同时使用 `datetime.utcnow()`、`datetime.now()`、固定 `timedelta(hours=8)` 和 `app/timeutil.py`。`/programs/unreported` 使用系统本地日期，而仪表盘/Bark 使用时区 helper；浏览器和容器时区改变时可能得出不同“今天”。

**建议方案**

- 数据库存储统一为 UTC；在现有 naive 列兼容期内明确“naive 即 UTC”。
- 所有业务日判断统一调用 `local_today_str()` / `is_same_local_day()`。
- 所有 API 时间输出带 `Z` 或明确 offset，禁止裸 ISO naive 字符串。
- 上报的 `execution_time` 若无时区，明确按哪一时区解析；建议脚本发带 offset 的 ISO 8601。
- 移除散落的固定 `+8h`，建立跨午夜、夏令时无关和历史 naive 数据测试。

**验收标准**

- 仪表盘、未上报列表、Bark 和积分今日变化在同一时刻返回一致日期判断。
- 服务器运行在 UTC 或 Asia/Shanghai 时结果一致。

### OPT-009：优化积分历史与“最新余额”查询

**现状证据**

- `fetch_points_history_grouped()` 为所有账号加载全部历史，再在 Python 中分组。
- `build_account_points_summary()` 为每个程序遍历历史计算最新值和前一日基线。
- 单程序排行也会加载该程序全部历史。
- 日志裁剪在每次上报后通过 `OFFSET max_entries` 找待删 ID，历史越大越昂贵。

**优化方向**

分阶段实施，先测量再改：

1. 建立数据规模基准和 `EXPLAIN QUERY PLAN`；记录接口响应时间、查询数和峰值内存。
2. 使用 SQL window function 或子查询一次取“每账号 × 程序最新记录”，并单独取今日之前的最近记录用于 diff。
3. 增加组合索引：
   - `points_history(program_id, wechat_id, report_time DESC, id DESC)`
   - `points_history(wechat_id, program_id, report_time DESC, id DESC)`
   - `stock_history(program_id, product_id, change_time DESC, id DESC)`
4. 若历史量持续增长，新增 `current_balances` 当前快照表：上报事务同时更新快照，历史仅供趋势/审计；列表接口不再扫描历史。
5. 把日志裁剪改为定时/批量任务，按已索引时间或 ID 分批删除，避免每次上报都扫描尾部。
6. “最新”使用 `(report_time, id)` 稳定排序，处理相同时间戳。

**验收标准**

- `/accounts`、`/points`、程序排行不再把全部历史加载进 Python。
- 在约定压测数据集上，响应时间和内存有可重复基准；建议目标为常用列表 p95 < 500 ms，但最终阈值应按真实部署硬件确认。
- 新旧查询对同一 fixture 的结果完全一致。

### OPT-010：库存中心真实分页与首屏性能

**现状证据**

- `/stock/center` 返回全部可见商品；`StockPage.vue` 收到全部数据后再执行映射、中文排序、筛选，所谓无限加载只是 `filteredProducts.slice(0, visibleCount)`，没有减少数据库查询、网络传输或 JSON 解析量。
- 页面 `onMounted()` 在库存中心完成后，还会自动请求已隐藏列表和**全部**已下架商品；打开对应抽屉时 watcher 又会再次请求，存在无用和重复加载。
- `stockCenterCache` 位于组件实例中，离开 `/stock` 后随组件卸载销毁，因此“60 秒缓存”不能让再次进入页面立即显示。
- `/stock/center` 为每个程序重新扫描一次全部商品来生成 `programs` 汇总，复杂度接近 `程序数 × 商品数`；当前 `StockPage.vue` 并未读取响应中的 `programs` 字段，这部分计算和传输没有实际消费者。
- 路由守卫在缓存过期后会先等待 `/access/status`，首次进入还要加载懒加载路由 chunk；二者是次要串行等待，应在核心数据链路缩小后再优化感知体验。

**当前数据实测基线（2026-09-18，只读测量）**

- 数据库共有 19,513 个商品，其中默认可见 9,248 个、已下架 10,260 个、手动隐藏 5 个。
- 当前 `/stock/center` 等价响应约 3.56 MiB 原始 JSON、0.70 MiB gzip。
- 页面自动加载的已下架响应约 2.97 MiB 原始 JSON、0.60 MiB gzip。
- 一次进入页面会处理约 19,508 个商品对象，合计约 6.53 MiB 原始 JSON、1.30 MiB gzip，尽管首屏只展示 20 条。
- 现有查询计划会扫描可见商品并使用临时 B-tree 排序；最新余额查询缺少 `(program_id, wechat_id, report_time, id)` 组合索引。
- 20 条首屏商品的近似响应仅约 7.1 KiB 原始 JSON、1.3 KiB gzip；与全量响应相比可减少约 99.8% 的传输量。

**第一阶段实测结果（当前工作区）**

- 在同一份本地数据库、ASGI 直连测量中，新的 `/stock/center?page=1&size=20` 返回约 8.2 KiB 原始 JSON、2.5 KiB gzip，接口耗时约 105 ms；仍需在真实局域网/部署设备上做 p95，而不是把本地单次耗时当作最终指标。
- 新的 `/stock/off-shelf?page=1&size=50` 返回 50 个商品（按程序分组显示为最多 20 个分组），约 17.1 KiB 原始 JSON、5.4 KiB gzip，接口耗时约 37 ms；打开抽屉后可继续点击加载下一页。
- 与基线的全量默认进入相比，首屏传输和 JSON 解析不再随 9,248 个可见商品线性增长；下架明细也不再一次性返回 10,260 个商品。

**建议方案**

1. **建立真实的库存分页契约**
   - `/stock/center`（或兼容期内新增 v2 路由）接受 `page/size/q/tag/status/price_mode/cash_max/sort`。
   - 响应只返回当前页商品，并同时提供 `total`、汇总计数、`available_tags`、`hidden_total`、`off_shelf_total` 和 `revision`。
   - 搜索、标签、库存状态、价格类型、现金上限和“可兑换”筛选移到服务端；前端无限滚动只追加下一页。
   - 删除前端未使用的 `programs` 响应，或将确有用途的程序聚合改为单次 `GROUP BY`。

2. **优化 SQL 与索引**
   - 合并积分/现金最高余额的重复查询，避免扫描两次相同历史范围。
   - 经正式迁移把 `is_hidden/is_unlisted` 回填并约束为非空，使查询使用简单等值条件而不是 `OR ... IS NULL`。
   - 基于 `EXPLAIN QUERY PLAN` 验证后增加组合索引，候选为：
     - `points_history(program_id, wechat_id, report_time DESC, id DESC)`
     - `products(is_hidden, is_unlisted, points, id)`
   - 当前数据只读副本中，前一个索引可把合并后的最新余额查询从几十至上百毫秒量级降低到约 5 ms；正式结果仍须在目标设备和真实 ORM 路径复测。
   - 第一阶段用 `LIMIT/OFFSET` 足够；只有深页性能实测不达标时再改游标分页。

3. **按需加载抽屉数据**
   - 首屏只获取隐藏/下架数量，不获取商品明细。
   - 仅在用户首次打开抽屉时请求明细；已下架接口也必须支持服务端分页与搜索。
   - 避免 `onMounted()` 和 drawer watcher 对同一数据重复请求。

4. **使用可失效的跨页面缓存**
   - 把第一页、汇总和筛选条件移到独立 store/module，使缓存跨组件卸载继续存在；不要把几千条完整数据同步写入 `localStorage`。
   - 页面再次进入时先展示缓存，再后台静默刷新（stale-while-revalidate），不要先清空为白屏。
   - 每次积分上报、库存上报、隐藏/恢复/重新上架时更新统一的 `inventory_view_revision`；缓存、ETag 和前端状态据此失效。

5. **缩短路由阶段的感知等待**
   - 保留现有动态导入；在登录成功、仪表盘空闲或库存菜单 hover 时预加载 `StockPage` chunk。
   - 给 router-view 增加全局导航骨架/进度反馈，使组件 chunk 和路由守卫等待期间不出现纯白区域。
   - 完成 OPT-002 的服务端统一鉴权后，再把 `/access/status` 从每次过期后的阻塞式路由检查改为后台复验 + 全局 401 处理，不能以降低白屏为由先削弱鉴权。

6. **控制长时间滚动后的 DOM 数量**
   - 首屏问题由服务端分页解决；当用户连续加载很多页时，再引入虚拟网格，只渲染视口附近商品卡。
   - 保留现有 `shallowRef`，避免重新引入大数组深层响应式开销。

`/programs` 当前只有约 222 条记录，其 Python 全量排序不是库存白屏主因；程序列表服务端分页仍应做，但优先级低于库存中心链路，可在本项后半段或独立 PR 处理。搜索所需拼音可在程序更新时缓存 `name_pinyin/name_initials`。

**验收标准**

- 首次进入库存页不再请求全部在架或全部下架商品；首屏商品接口最多返回约定页大小（默认 20）。
- 默认首屏压缩响应建议控制在 20 KiB 内；不得随商品总量线性增长。
- 重复进入页面可立即展示未过期或可后台复验的第一页缓存，不出现空白内容区。
- 局域网目标环境首批商品 p95 建议不高于 300 ms，远程常规网络建议不高于 800 ms；最终阈值以部署硬件和链路基准确认。
- 隐藏/下架抽屉只在打开后加载，支持分页，并且相同操作不会触发重复请求。
- 常规分页 SQL 只读取当前页和必要聚合；`EXPLAIN QUERY PLAN` 不再对默认首屏执行无索引的全表排序。
- 搜索、标签、价格、库存、可兑换筛选和原有排序都有新旧语义对照测试。
- 使用浏览器 Network/Performance 面板记录冷进入、热进入、筛选和连续下拉四条基线，PR 中附改造前后数据。

### OPT-011：修正同步 I/O、路由并发与后台任务生命周期

**现状证据**

- `web.py` 大部分 endpoint 声明为 `async def`，内部却使用同步 SQLAlchemy、`requests`、sqlite3、shutil；这些调用会直接阻塞 event loop。
- `images.py` 在 async route 中同步复制上传文件。
- Bark/QingLong 使用 daemon thread + `time.sleep()`，没有显式 shutdown、stop event 或锁释放流程。
- 启动使用已逐步弃用的 `@app.on_event("startup")` 模式。

**建议方案**

- 短期：纯同步 SQLAlchemy/文件 I/O 路由改为普通 `def`，让 FastAPI 在线程池执行；真正 async 的上传分块使用 await，重 CPU/文件操作显式 offload。
- 中期二选一：保持完整同步栈，或整体迁移 async SQLAlchemy + `httpx.AsyncClient`；不要半同步半异步。
- 使用 FastAPI lifespan 管理 scheduler 启停、stop event、线程 join、HTTP client 和文件锁。
- 对外部 QingLong/Bark 调用配置连接复用、超时、有限重试和退避；重试仅用于幂等操作。
- 避免 blocking QingLong 同步占用列表请求；默认保持后台同步，UI 展示“数据时间/同步中”。

**验收标准**

- 慢 QingLong/Bark 请求不会卡住无关 API。
- 应用关闭时后台线程能在有限时间内退出并释放锁。
- 并发 smoke test 下没有 event-loop 长时间阻塞告警。

**第一阶段实施记录（2026-09-18）**

- 大部分同步数据库/文件/网络 I/O 路由已改为普通 `def`，交给 FastAPI 线程池执行。
- FastAPI 已改用 lifespan；Bark/QingLong 调度器使用 `threading.Event` 停止、有限时间 join，并在成功退出后释放进程锁。
- 外部 HTTP 连接复用、有限重试和退避仍未实施；当前继续依赖每次请求的显式超时。

## 5. P1：运行、部署与可观测性

### OPT-012：健康检查、结构化日志、审计与指标

**建议方案**

- 增加 `/health/live` 与 `/health/ready`：ready 至少检查数据库可读、迁移版本正确；前端是否存在可单独显示，不要让纯 API 健康检查误判。
- 用标准 `logging` 替换 `print()`，统一 JSON/文本字段：时间、级别、request_id、route、duration、status、program_id、batch_id；严禁记录 secret/token。
- 请求中间件生成/透传 request ID，错误响应返回可查询的 ID。
- 记录高风险审计事件：登录失败、配置修改、数据库导入、程序/账号删除、批量 cron 更新。
- 最少采集：请求量/延迟/5xx、SQLite lock 错误、上报成功/失败、未上报数量、QingLong/Bark 同步状态、调度线程存活。
- Docker Compose 加 healthcheck；部署系统依据 ready 状态重启/摘流，而不是只看进程存在。

**验收标准**

- 一次线上错误可通过 request ID 定位到完整上下文，但日志不泄露凭据。
- 调度线程停止、数据库不可写、QingLong 连续失败能够被监控发现。

**第一阶段实施记录（2026-09-18）**

- 已保留 live/ready 健康检查，并在 API/健康请求上生成或透传安全的 `X-Request-ID`。
- 已记录 `request_id`、method、route、status、duration_ms 等 key-value 请求日志，不记录 query string 或凭据。
- 高风险审计事件、指标导出和统一 JSON 日志格式仍未实施。

### OPT-013：可复现、非 root、自包含部署

**现状证据**

- Docker 镜像不包含前端构建产物，运行依赖宿主机先构建并挂载 `frontend/dist`。
- 容器和 systemd 示例均以 root 运行；systemd 使用 2 workers，Compose 默认 1 worker。
- systemd 绕过 `start.sh`/entrypoint 的初始化和前端检查。

**建议方案**

- 使用多阶段 Dockerfile：Node 阶段 `npm ci && npm run build`，Python 阶段复制 `dist`，生成可独立运行的不可变镜像。
- 开发热挂载与生产部署拆成不同 compose 文件；生产不挂载源代码。
- 创建非 root 用户，只授予 `data/logs/static/uploads` 必要写权限。
- 固定基础镜像版本或 digest；记录镜像来源和升级流程。
- 统一 worker 策略为 SQLite 默认 1；如需要水平扩展，先迁移 PostgreSQL/外部调度器。
- systemd 调用统一启动脚本、设置 `EnvironmentFile`、正确工作目录和最小权限。
- 增加自动备份、保留和恢复演练，而不只提供手工下载按钮。

**验收标准**

- 新机器只需环境变量和持久化卷即可启动，不要求宿主机安装 Node。
- 容器进程不是 root；代码目录运行时只读。
- Docker 与 systemd 的端口、worker、时区、建库和前端检查行为一致。

**第一阶段实施记录（2026-09-18）**

- `Dockerfile` 已使用 `npm ci` + `npm run build` 生成前端，再将产物复制到 Python runtime；运行时使用固定 UID/GID 的 `app` 用户，并预创建可写的 `data`、`logs`、`static/uploads` 目录。
- `docker-compose.yml` 已移除 `./frontend/dist` 挂载；端口 `5711:5711`、`.env`、`TZ`、`UVICORN_WORKERS`、`host.docker.internal`、ready healthcheck 及持久化目录保持不变，`app`/`scripts` 源码挂载改为只读以兼容现有 Windows 工作流。
- `.dockerignore` 已排除宿主机前端构建产物、`node_modules`、运行数据和上传文件，避免构建结果依赖宿主机状态。
- 已验证：`python -m compileall -q app tests` 通过；`pytest -q tests` 为 **16 passed**；前端 `npm ci`、`npm run build` 通过；启动脚本 Shell 语法检查通过；`git diff --check` 无空白错误。
- 未执行：Docker build 与 `docker compose config`。当前环境找不到 Docker CLI，因此镜像层、容器内实际 UID 和 Windows/宿主机 bind mount 写权限仍需有 Docker 的目标机器复核。

## 6. P2：前端结构与体验

### OPT-014：拆分大型 Vue 页面与共享业务逻辑

**现状证据**

- `ProgramsPage.vue` 约 3105 行。
- `style.css` 约 2358 行。
- `StockPage.vue` 约 1580 行；`QinglongCronsPage.vue` 约 1293 行；`UsersPage.vue` 约 974 行。
- 商品价格/可兑换判断、日期格式化、手机号展示、无限滚动和响应式断点在多个页面重复实现。

**建议拆分顺序**

1. 先抽纯函数并写测试：`formatMoney`、`formatProductPrice`、`isRedeemable`、`parseCron`、`formatApiDate`、手机号判断。
2. 抽 composables：
   - `useViewport()`
   - `useInfiniteScroll()`
   - `useProgramFilters()`
   - `useAccessSession()`
   - `useStockFilters()`
3. 抽展示组件：程序卡、详情弹窗、库存弹窗、商品卡、筛选栏、状态指标卡。
4. 页面只保留数据编排和路由状态，单组件建议控制在可审查范围内，而不是追求机械行数指标。
5. 把全局设计 token、布局样式和页面 scoped 样式分层；删除重复选择器。

**验收标准**

- 拆分前后页面视觉和交互一致。
- 核心纯函数有单测；共享逻辑只有一个实现。
- Programs/Stock 页面不再同时承担 API、状态、业务计算和全部渲染细节。

### OPT-015：统一请求、缓存、响应式断点和错误处理

**现状证据**

- `PointsPage.vue:436` 的 `computed(() => window.innerWidth...)` 没有响应式依赖，窗口变化后抽屉尺寸不会可靠更新。
- 多个页面各自注册 resize/IntersectionObserver、格式化错误信息和 401 行为。
- Programs 页面把整页程序数据写进 sessionStorage，没有 TTL/版本；访问身份或后端数据变化后可能展示陈旧数据。

**建议方案**

- 用一个响应式 `useViewport()` 提供断点，统一注册/移除监听。
- Axios 层统一：401 会话失效、错误格式、request ID、超时分类、可取消请求。
- 搜索和筛选请求使用 debounce + AbortController，避免旧响应覆盖新状态。
- 页面状态缓存只保存筛选条件和滚动位置；如缓存数据，必须带 schema version、账号/session 标识、后端更新时间与 TTL。
- 对有副作用按钮统一 pending 防重复、成功后局部更新或可控重载。
- 为键盘操作、焦点回收、dialog/drawer、颜色对比度补基础可访问性检查。

**验收标准**

- 桌面/移动断点切换无需刷新即可更新布局。
- 密钥失效后任意 API 401 都会回到访问页。
- 快速连续筛选不会出现旧结果覆盖新结果。

### OPT-016：规范长期数据模型

这些项目改动较大，应在测试、迁移和性能基线建立后再做：

- **金额精度**：`cash` 当前使用 Float。若金额参与严格比较/结算，迁移为定点整数（分）或 `Decimal/Numeric`；API 边界明确舍入规则。
- **标签**：当前 JSON 字符串 + LIKE 搜索。规模增大后可建 `tags` / `program_tags` 表，获得精确查询、唯一约束和统计。
- **账号身份**：手机号和 `wechat_id` 混在主身份中，且手机号不唯一。可引入账号主体 + 外部身份表，明确一个手机号是否允许绑定多个微信账号。
- **当前余额快照**：历史表与当前状态分离，降低所有列表查询成本。
- **库存快照可信度**：非空但残缺的爬取仍会批量下架。可增加 `snapshot_id`、`is_complete`、预期商品数/异常阈值或两次确认机制。
- **QingLong 映射**：当前名称启发式可能误匹配。增加人工锁定映射、匹配分数、未匹配/冲突列表和 dry-run。
- **系统设置**：把 secret 与普通设置分离，支持加密存储、轮换和审计。

## 7. P2：工程质量与文档

### OPT-017：前端测试、lint、格式化和依赖治理

**建议方案**

- 后端：Ruff（格式 + lint）、可选 mypy/pyright；先修新增代码和关键模块，再逐步扩大。
- 前端：ESLint + Prettier；Vitest + Vue Test Utils 测纯函数/组件；Playwright 做登录、列表、设置、库存的少量端到端 smoke test。
- 使用 `npm ci` 保证 package-lock 生效；后端考虑生成可复现 lock，并在受控 PR 中升级依赖。
- CI 加依赖漏洞扫描和许可证检查；升级必须伴随构建和测试，而不是自动直接部署。
- 为 API 响应补 Pydantic response model，减少前后端字段漂移；可进一步生成前端类型或逐步迁移 TypeScript。

### OPT-018：收敛文档漂移与废弃代码

**已知漂移/遗留**

- README/CLAUDE 的部分描述称自动模式会由程序列表触发后台 QingLong 同步；当前代码是 scheduler-only，`trigger_background_sync()` 没有调用点。
- `CLAUDE.md` 建议依赖 Vue auto-import，但当前多数页面显式导入 Vue API；应选定一种团队约定。
- systemd 的 workers 与 Compose 推荐值不一致。
- `HelloWorld.vue`、Vue/Vite logo 资源仍是模板遗留。
- README 的页面/API 列表容易落后于实际 router。

**建议方案**

- 以代码生成的 OpenAPI 为 API 基线，手工文档只解释业务语义和示例。
- 每次改变上报契约，强制同步 `schemas.py`、服务逻辑、模板、库存文档和回归测试。
- 删除确认无引用的模板文件；CI 增加未使用资源检查可作为后续项。
- 在 PR 模板加入“迁移、API 文档、上报模板、移动端、旧数据库兼容”检查项。

## 8. 推荐实施阶段

### 阶段 A：安全止血与测试底座

顺序：OPT-001 → OPT-005 基础框架 → OPT-003 → OPT-002。

完成标准：

- 仓库无真实凭据；上报 Token 已改为无默认值的专用凭据，或已由经过验证的可信网络边界替代。
- 积分上报、库存上报和图片上传不能被不可信网络匿名调用。
- 核心上报、库存快照、跨程序余额和鉴权有自动化测试。
- 开启访问保护后，服务端 API 无绕过路径。

### 阶段 B：数据库可靠性

顺序：OPT-004 → OPT-006 → OPT-007 → OPT-008。

完成标准：

- 数据库恢复有维护锁、原子替换和 WAL 测试。
- 正式迁移替代请求期 DDL，外键/删除策略清晰。
- 输入与上传有限制，时间语义一致。

### 阶段 C：性能与并发

顺序：先记录库存页冷/热加载基准，再优先做 OPT-010；随后完成 OPT-009 的其余历史查询优化，再做 OPT-011。OPT-010 所需的两条针对性索引随库存分页 PR 一并迁移，不必等待 OPT-009 全部完成。

完成标准：

- 常用列表不扫描全部历史/商品。
- 库存首屏使用真实服务端分页，进入页面不再自动下载全部下架商品，重复进入可先显示跨页面缓存。
- 慢外部服务不会阻塞无关请求；后台线程可优雅退出。

### 阶段 D：部署与可观测性

实施 OPT-012、OPT-013。

完成标准：

- 有 live/ready、结构化日志、审计和最小告警。
- 生产镜像自包含、非 root、配置一致且可回滚。

### 阶段 E：前端与长期模型

分批实施 OPT-014～OPT-018，每次只拆一个稳定边界，避免一次性重写。

## 9. 建议的首批 PR 切分

1. `security/define-ingest-auth-boundary`：确认上报 Token 去留；实施 `INGEST_TOKEN` 收敛或可信网络边界，同时完成 `.env.example`、凭据失效/迁移和 secret scan。
2. `test/backend-contract-baseline`：临时 SQLite、上报/库存/鉴权基础测试。
3. `fix/program-stock-max-balance`：修正 program join + 回归测试。
4. `security/protect-ui-api`：统一 access dependency、Axios 401、限流策略。
5. `hardening/database-restore`：维护锁、上传限制、checkpoint、原子替换、集成测试。
6. `db/versioned-migrations`：迁移框架、foreign_keys、孤儿清理、索引。
7. `perf/current-balances-query`：最新余额 SQL/快照表与基准。
8. `perf/server-side-stock-pagination`：库存中心真实分页、汇总/数量元数据、下架列表懒加载与分页、组合索引、跨页面 stale-while-revalidate 缓存及冷/热加载基准。
9. `ops/health-logging-image`：健康检查、日志、生产镜像和非 root。
10. `frontend/shared-domain-utils`：先抽纯函数和测试，再逐页拆组件。

## 10. 每个优化 PR 的通用验收清单

- [ ] 没有读取或改写真实 `data/database.db`、真实 `.env`、QingLong/Bark 线上配置。
- [ ] 相关旧数据库升级路径已验证，或明确说明不涉及 schema。
- [ ] 新行为有自动化测试；bug 修复包含能复现旧问题的测试。
- [ ] API 请求/响应变化已同步前端、OpenAPI/文档和脚本模板。
- [ ] 对桌面和移动端至少完成关键路径 smoke test。
- [ ] 涉及时间时覆盖 Asia/Shanghai 跨日边界。
- [ ] 涉及删除/恢复时有备份、失败回滚和幂等性说明。
- [ ] 日志不包含 token、access key、Client Secret、Bark Device Key 或上传数据库内容。
- [ ] `python -m compileall`、后端测试、`npm run build` 全部通过。
- [ ] 更新根目录 `CODEX_REPO_MEMORY.md` 中受影响的事实和约束。

## 11. 暂不建议的做法

- 在没有测试和迁移基线前一次性重写后端或改用另一套数据库。
- 只在前端隐藏按钮来“保护”写接口。
- 为解决 SQLite 锁问题盲目增加 uvicorn workers。
- 继续把新列靠请求时 `ALTER TABLE` 长期维护。
- 为性能先引入缓存层，却不先确认最慢 SQL 和全量加载点。
- 在库存全量快照语义未加完整性信号前，简单取消所有下架检测；这会重新引入失效商品长期残留。
- 把真实备份库、日志、上传图片或密钥加入测试 fixture/仓库。
